import { type Input } from '../input/input'
import { type SearchLabel } from '../searchLabel/searchLabel'

export interface SearchPropsType {
  image: string
  input: Input
  searchLabel: SearchLabel
}
