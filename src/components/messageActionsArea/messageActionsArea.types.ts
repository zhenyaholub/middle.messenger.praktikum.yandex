import { type Input } from '../input/input'
import { type RoundedButton } from '../roundedButton/roundedButton'

export interface MessageActionsAreaPropsType {
  image: string
  input: Input
  submit: RoundedButton
}
