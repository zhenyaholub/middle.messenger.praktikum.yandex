import { Input } from "../input/input";
import { RoundedButton } from "../roundedButton/roundedButton";

export type MessageActionsAreaPropsType = {
  image: string;
  input: Input;
  submit: RoundedButton;
};
